name: Test Action  # Ensure this is at the start of the file with no hidden characters

on:
  push:
    branches:
      - main  # Ensure proper indentation

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 22f2000905@ds.study.iitm.ac.in
        run: echo "Hello, world!"
